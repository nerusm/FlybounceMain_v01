<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <group android:checkableBehavior="single">
        <item
            android:id="@+id/nav_add_member"
            android:icon="@drawable/ic_person_add_black"
            android:title="Add" />
        <item
            android:id="@+id/nav_list_groups"
            android:icon="@drawable/ic_group_add_black"
            android:title="Groups" />
        <item
            android:id="@+id/nav_showmember"
            android:icon="@drawable/ic_person_black_24dp"
            android:title="Show Member" />
        <item
            android:id="@+id/nav_listmembers"
            android:icon="@drawable/ic_menu_manage"
            android:title="List" />
    </group>

    <item android:id="@+id/search"
        android:title="@string/search_title"
        android:icon="@drawable/ic_search_black_24dp"
        android:showAsAction="collapseActionView|ifRoom"
        android:actionViewClass="android.widget.SearchView"
        tools:ignore="AppCompatResource" />

    <item android:title="Communicate">
        <menu>
            <item
                android:id="@+id/nav_share"
                android:icon="@drawable/ic_menu_share"
                android:title="Share" />
            <item
                android:id="@+id/nav_send"
                android:icon="@drawable/ic_menu_send"
                android:title="Send" />
        </menu>
    </item>



</menu>
